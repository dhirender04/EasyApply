<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EasyApply" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.EasyApply" parent="Base.Theme.EasyApply" />

    <style name="CustomBoldTheme">
        <item name="fontFamily"/>
    </style>
    <style name="DefaultButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:backgroundTint">?attr/colorPrimary</item> <!-- Default background color -->
        <item name="android:textColor">?attr/colorPrimary</item> <!-- Default text color -->
    </style>

    <!-- Custom Outlined Button Style -->
    <style name="OutlinedButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="strokeColor">?attr/colorPrimary</item> <!-- Outline color -->
        <item name="strokeWidth">2dp</item> <!-- Outline width -->
        <item name="cornerRadius">4dp</item> <!-- Corner radius -->
        <item name="android:textColor">?attr/colorPrimary</item> <!-- Text color -->
        <item name="android:backgroundTint">@android:color/transparent</item> <!-- Transparent background -->
    </style>
</resources>